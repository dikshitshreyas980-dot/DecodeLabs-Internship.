services:
  - type: web
    name: decodebot-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "cd backend && gunicorn app:create_app()"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 10000
